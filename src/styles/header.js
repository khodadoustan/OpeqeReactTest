import {makeStyles} from "@material-ui/core";

export default useStyle = makeStyles({
    header: {
        position: "fixed",
        zIndex: 99,
        top: 0,
        width: "100%",
        left: 0,
        textAlign: "center",
        transition: "background-color .4s"
    }
});
